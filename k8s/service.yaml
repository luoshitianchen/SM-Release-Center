apiVersion: v1
kind: Service
metadata:
  name: sm-release-center
  namespace: sm
  labels:
    app: sm-release-center
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-release-center
  ports:
  - name: http
    port: 8550
    targetPort: http
    protocol: TCP
